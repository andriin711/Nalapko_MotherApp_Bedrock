{
  "name": "next-bedrock-agent",
  "private": true,
  "scripts": {
    "agent": "node agent/cli.mjs",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "planner": "uvicorn planner.app:app --host 0.0.0.0 --port 8080 --reload"
  },
  "dependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.13"
  }
}